https://youtu.be/lJbf0J9rRzE




#terraform code 

resource "aws_instance" "web" {
  ami           = "ami-00ee4df451840fa9d" #Amazon Linux AMI
  instance_type = "t2.micro"
  security_groups = [aws_security_group.TF_SG.name]
  #first method is to create a key pait through AWS console (e.g "TF_key")and add it in configuration file.
  key_name = "TF_key"
  

  tags = {
    Name = "Terraform Ec2"
  }
}

#keypair second method for Key_pair is to create it through terraform.

resource "aws_key_pair" "TF_key" {
  key_name   = "TF_key"
  public_key = tls_private_key.rsa.public_key_openssh
}

resource "tls_private_key" "rsa" {
  algorithm = "RSA"
  rsa_bits  = 4096
}

resource "local_file" "TF-key" {
    content  = tls_private_key.rsa.private_key_pem
    filename = "tfkey"
}
